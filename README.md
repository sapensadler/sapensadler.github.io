JKA WF England — Eleventy Starter

Quick start:

1. npm install
2. npm run start  # develop with eleventy --serve
3. npm run build  # build static site in _site/

This scaffold includes a basic header (logo + nav) and footer, and pages: Home, About, Clubs, Events, Contact.

